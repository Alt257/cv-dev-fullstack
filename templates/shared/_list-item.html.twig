{# Élément de liste avec :
    - une icone
    - le texte avec un lien si disponible
#}

{% include 'shared/_icon.html.twig' with {
    class: itemType ~ '-icon',
    icon: item.icon,
    iconImage: item.iconImage,
} only %}

{% include 'shared/_text.html.twig' with {
    href: item.href,
    text: item.text,
} only %}